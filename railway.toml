[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "backend"
source = "backend/"

[[services]]
name = "mongodb"
image = "mongo:latest"